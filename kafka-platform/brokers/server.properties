# Hard 설정 — 변경 금지
# min.insync.replicas=2        # 로컬은 브로커 1대이므로 비활성. 운영(3대)에서 활성화
# default.replication.factor=3 # 운영에서 활성화

# 토픽 자동 생성 비활성 — 반드시 선언 후 사용
auto.create.topics.enable=false

# 로그 보관
log.retention.hours=168
